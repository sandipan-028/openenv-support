[project]
name = "openenv-support"
version = "0.1.0"
description = "OpenEnv support environment"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"